version: "3.9"

services:
  app:
    build: .
    image: yelp-incar-recsys:latest
    container_name: yelp-incar-recsys
    ports:
      - "8501:8501"
    environment:
      # Optional Groq key for the LLM "why" annotations. Falls back to
      # a deterministic template if unset.
      - GROQ_API_KEY=${GROQ_API_KEY:-}
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    restart: unless-stopped
